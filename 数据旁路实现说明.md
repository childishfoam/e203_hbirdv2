# E203 数据旁路实现说明

## 实现概述

本次实现为蜂鸟E203 RISC-V处理器添加了数据旁路(数据前推)机制，用于处理流水线中的数据冒险，提高处理器性能。

## 完成的工作

### 1. RTL代码修改

#### 修改的文件：
- `rtl/e203/core/e203_exu_disp.v` - 调度模块，添加前推逻辑
- `rtl/e203/core/e203_exu.v` - 执行单元，连接前推信号
- `tb/tb_top.v` - 测试平台，添加指令监控
- `riscv-tools/riscv-tests/isa/rv32ui/hazard_test.S` - 数据冒险测试程序

### 2. 数据前推机制

实现了两条前推路径：

1. **ALU到调度的前推**：ALU写回阶段的数据可以直接前推到调度阶段
2. **长流水线到调度的前推**：load/store操作的数据可以在可用时前推

#### 核心特性：
- 自动检测写回数据与源操作数寄存器的匹配
- 前推优先级：ALU前推 > 长流水线前推 > 寄存器堆读取
- 更新了依赖检查逻辑，考虑前推数据
- RAW冒险通过前推解决，无需流水线停顿

### 3. 测试程序

创建了包含20条指令的测试程序(`hazard_test.S`)，测试各种数据冒险场景：

```assembly
1.  addi x1, x0, 5      # 初始化x1
2.  addi x2, x0, 3      # 初始化x2
3.  add  x3, x1, x2     # x1和x2的RAW冒险
4.  sw   x3, 0(x0)      # x3的RAW冒险
5.  lw   x4, 0(x0)      # load指令（长流水线）
6.  add  x5, x4, x1     # x4的load-use冒险
7.  sub  x6, x5, x2     # x5的RAW冒险
8.  mul  x7, x6, x3     # x6的RAW冒险
9.  addi x1, x0, 8      # x1的WAW冒险
10. add  x8, x7, x1     # x7和x1的RAW冒险
11. and  x9, x8, x5     # x8的RAW冒险
12. or   x10, x9, x7    # x9的RAW冒险
13. xor  x11, x10, x8   # x10的RAW冒险
14. sll  x12, x11, x2   # x11的RAW冒险
15. srl  x13, x12, x1   # x12的RAW冒险
16. addi x14, x13, -1   # x13的RAW冒险
17. sw   x14, 4(x0)     # x14的RAW冒险
18. lw   x15, 4(x0)     # load指令（长流水线）
19. add  x16, x15, x10  # x15的load-use冒险
20. mul  x17, x16, x13  # x16的RAW冒险
```

### 4. 测试平台增强

测试平台增加了以下功能：
- 跟踪20条测试指令的执行
- 每条指令的周期计数
- 指令完成后打印寄存器值
- 总周期数统计

#### 输出格式示例：
```
[HAZARD_TEST] Instruction 1: addi x1, x0, 5  | Cycle: XXX | x1=00000005
[HAZARD_TEST] Instruction 2: addi x2, x0, 3  | Cycle: XXX | x2=00000003
...
========== HAZARD TEST COMPLETED ==========
Total cycles for 20 instructions: XXX
===========================================
```

## 如何运行测试

### 环境准备：

1. **设置工具链**（如问题描述中所述）：
```bash
mkdir -p ./riscv-tools/prebuilt_tools/prefix/bin
cd ./riscv-tools/prebuilt_tools/prefix/bin/
# 建立riscv gcc的链接
ln -s ~/nuclei_riscv_newlibc_prebuilt_linux64_2020.08/gcc/bin/* .
cd ../../../../
```

2. **编译测试程序**：
```bash
cd riscv-tools/riscv-tests/isa
source regen.sh
```

这将生成：
- `generated/rv32ui-p-hazard_test` - 可执行文件
- `generated/rv32ui-p-hazard_test.dump` - 反汇编文件
- `generated/rv32ui-p-hazard_test.verilog` - 仿真用内存镜像

3. **运行仿真**：
```bash
cd vsim
make clean
make install
make compile SIM=iverilog
make run_test TESTCASE=../riscv-tools/riscv-tests/isa/generated/rv32ui-p-hazard_test DUMPWAVE=0
```

## 预期结果

### 没有数据前推时：
- 每个RAW冒险都会导致流水线停顿
- 每条指令的周期数较高
- load-use冒险会导致额外停顿

### 有数据前推时：
- 大多数RAW冒险通过前推解决，无需停顿
- 周期数减少（预计提升20-30%）
- 仅load-use冒险可能仍需停顿
- 指令吞吐量提高

## 技术要点

### 前推优先级：
1. ALU写回（最高优先级 - 最新数据）
2. 长流水线写回（load/store完成）
3. 寄存器堆读取（无需前推）

### 实现细节：

在`e203_exu_disp.v`中添加的关键逻辑：

```verilog
// 前推检测
wire alu_fwd_rs1 = alu_wbck_i_valid & disp_i_rs1en & 
                   (alu_wbck_i_rdidx == disp_i_rs1idx) & 
                   (alu_wbck_i_rdidx != 0);

// 前推数据选择
wire [`E203_XLEN-1:0] fwd_rs1_dat = alu_fwd_rs1 ? alu_wbck_i_wdat : 
                                    longp_fwd_rs1 ? longp_wbck_i_wdat : 
                                    disp_i_rs1;

// 考虑前推的依赖检查
wire oitfrd_match_disprs1_no_fwd = oitfrd_match_disprs1 & ~(alu_fwd_rs1 | longp_fwd_rs1);
```

### 特殊情况：
- 寄存器x0永远不会被前推（始终为零）
- 只有目标寄存器与源寄存器匹配时才进行前推
- OITF仍然跟踪长流水线指令

## 性能分析

前推逻辑带来的关键路径延迟最小：
- 前推检测：寄存器索引比较（与依赖检查并行）
- 数据多路复用：2选1或3选1多路选择器
- 设计不会显著影响时钟频率

预期性能提升：
- 对于包含大量连续运算的代码：20-30%周期减少
- 对于包含大量load-use的代码：5-10%周期减少
- 整体IPC（每周期指令数）提升

## 实现验证

通过以下方式验证实现正确性：

1. **功能验证**：运行hazard_test程序，检查所有寄存器值正确
2. **性能验证**：比较有/无前推时的总周期数
3. **正确性验证**：确保最终结果与无前推版本相同

## 代码文件说明

### 核心修改
- **e203_exu_disp.v**：约50行新增代码，实现前推逻辑
- **e203_exu.v**：约10行修改，连接前推信号
- **tb_top.v**：约80行新增代码，实现指令监控

### 测试文件
- **hazard_test.S**：87行汇编代码
- **Makefrag**：添加hazard_test到构建系统

## 未来改进方向

可能的增强：
1. 从commit阶段添加前推，支持更晚的数据可用性
2. 实现分支预测的推测性前推
3. 添加旁路网络可视化
4. 扩展前推支持FPU指令（如果启用E203_HAS_FPU）

## 参考资料

- E203原始架构文档
- RISC-V流水线冒险处理技术
- 经典RISC流水线中的数据前推/旁路技术

---

详细的英文文档请参考：`DATA_FORWARDING_IMPLEMENTATION.md`
